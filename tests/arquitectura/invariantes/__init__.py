"""Invariantes de código declaradas por cada plan de la fase.

Cada plan agrega su propio `inv_01_0N.py` exponiendo `INVARIANTES` y nada más,
para que ningún plan de la misma ola edite el archivo de otro.
Reemplaza los criterios de aceptación basados en `grep`, que no son verificables
en la única plataforma de compuerta (Windows, D-54)."""
