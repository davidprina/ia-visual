---
phase: 01-n-cleo-evidencia-trazable-y-contratos-externos-congelados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
  - .gitignore
  - scripts/compuerta.py
  - .github/workflows/compuerta.yml
  - src/porteria/__init__.py
  - src/porteria/dominio/__init__.py
  - src/porteria/dominio/comun/__init__.py
  - src/porteria/dominio/evidencia/__init__.py
  - src/porteria/dominio/viaje/__init__.py
  - src/porteria/dominio/auditoria/__init__.py
  - src/porteria/dominio/identidad/__init__.py
  - src/porteria/aplicacion/__init__.py
  - src/porteria/aplicacion/puertos/__init__.py
  - src/porteria/aplicacion/puertos/entrada/__init__.py
  - src/porteria/aplicacion/puertos/salida/__init__.py
  - src/porteria/aplicacion/casos_de_uso/__init__.py
  - src/porteria/aplicacion/servicios/__init__.py
  - src/porteria/aplicacion/dto/__init__.py
  - src/porteria/infraestructura/__init__.py
  - src/porteria/infraestructura/video/__init__.py
  - src/porteria/infraestructura/imagen/__init__.py
  - src/porteria/infraestructura/persistencia/__init__.py
  - src/porteria/infraestructura/persistencia/sqlite/__init__.py
  - src/porteria/infraestructura/externos/__init__.py
  - src/porteria/infraestructura/externos/comun/__init__.py
  - src/porteria/infraestructura/externos/paljet/__init__.py
  - src/porteria/infraestructura/externos/balanza/__init__.py
  - src/porteria/infraestructura/externos/geomov/__init__.py
  - src/porteria/infraestructura/transporte/__init__.py
  - src/porteria/infraestructura/transporte/en_memoria/__init__.py
  - src/porteria/infraestructura/transporte/ipc/README.md
  - src/porteria/infraestructura/runtime/__init__.py
  - src/porteria/infraestructura/seguridad/__init__.py
  - src/porteria/infraestructura/configuracion/__init__.py
  - src/porteria/cli/__init__.py
  - src/porteria/cli/app.py
  - src/porteria/cli/ordenes/__init__.py
  - src/porteria/composicion/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/arquitectura/__init__.py
  - tests/arquitectura/test_dominio_aislado.py
  - tests/arquitectura/sonda_dominio_aislado.py
  - tests/arquitectura/test_sabotaje_compuerta_dominio.py
  - tests/arquitectura/test_invariantes_de_codigo.py
  - tests/arquitectura/invariantes/__init__.py
  - tests/arquitectura/invariantes/inv_01_01.py
  - tests/aceptacion/__init__.py
  - tests/aceptacion/test_rebanada_captura.py
  - tests/recursos/__init__.py
  - tests/recursos/videos/README.md
  - tests/dominio/__init__.py
  - tests/integracion/__init__.py
  - tests/contrato/__init__.py
  - tests/migracion/__init__.py
  - tests/lentas/__init__.py
autonomous: false
requirements: [NUC-01, DIS-06]
must_haves:
  truths:
    - "`uv run lint-imports --no-cache` devuelve 0 sobre el árbol limpio y 1 cuando alguien introduce cv2, onnxruntime o PySide6 en dominio/"
    - "`uv run --python 3.12 --isolated --no-project python tests/arquitectura/sonda_dominio_aislado.py` importa todo dominio/ en un entorno donde esos paquetes NO están instalados y devuelve 0 **sin necesidad de definir `PYTHONPATH`**: la sonda se auto-resuelve `src/` en `sys.path[0]` como primera sentencia ejecutable"
    - "`uv run pytest -q -m 'not lenta'` corre sobre una ruta de trabajo con espacios y acentos y no falla por encoding ni por largo de ruta; si se recolectaron 0 pruebas la sesión **falla**, nunca termina en verde"
    - "`uv run python scripts/compuerta.py` encadena las cuatro compuertas, devuelve el peor exit code de todas y trata «0 pruebas recolectadas» como fallo"
    - "La prueba de aceptación de la rebanada de captura existe, está marcada xfail(strict=True) y describe el resultado observable completo"
    - "Las invariantes de código de los diez planes de la fase se comprueban como aserciones de pytest multiplataforma en `tests/arquitectura/test_invariantes_de_codigo.py`, no con `grep`, que el shell de la plataforma de compuerta puede no tener"
    - "La rama `main` exige el check de `compuerta.yml` para poder fusionar, y el job `tanda-lenta` no figura entre los requeridos"
  artifacts:
    - path: "pyproject.toml"
      provides: "Proyecto uv con src layout, pytest ini options, contratos de import-linter, dependency-groups"
      contains: "[tool.importlinter]"
    - path: "tests/conftest.py"
      provides: "Fixture de sesión de ruta con espacios y acentos, motor SQLite, reloj determinista, video sintético"
      contains: "ñandú"
    - path: "tests/arquitectura/sonda_dominio_aislado.py"
      provides: "Sonda de runtime que importa todo dominio/ y falla si un módulo prohibido quedó en sys.modules"
    - path: "tests/arquitectura/test_sabotaje_compuerta_dominio.py"
      provides: "Prueba de la prueba: siembra un import cv2 en dominio/ y exige exit 1 de lint-imports"
    - path: "scripts/compuerta.py"
      provides: "Compuerta de CI en Windows que propaga el peor exit code"
    - path: ".github/workflows/compuerta.yml"
      provides: "Ejecución en integración continua sobre windows-latest"
    - path: "tests/aceptacion/test_rebanada_captura.py"
      provides: "Prueba de aceptación de la rebanada, escrita antes del código (D-56)"
    - path: "tests/arquitectura/test_invariantes_de_codigo.py"
      provides: "Comprobación multiplataforma de las invariantes de código que cada plan declara; reemplaza los criterios basados en grep"
      contains: "iter_modules"
    - path: "tests/arquitectura/invariantes/inv_01_01.py"
      provides: "Invariantes declaradas por este plan (pyproject sin licencias contagiosas ni dependencias prohibidas, punto de entrada con modo UTF-8, compuerta de licencias con lista blanca)"
  key_links:
    - from: "scripts/compuerta.py"
      to: "lint-imports / pytest / uv --isolated / pip-licenses"
      via: "subprocess con propagación del peor exit code"
      pattern: "lint-imports"
    - from: "pyproject.toml"
      to: "porteria.dominio"
      via: "contrato forbidden de import-linter"
      pattern: "forbidden_modules"
---

<objective>
Levantar el andamiaje ejecutable del proyecto y sus cuatro compuertas automáticas, y escribir —antes
que el código— la prueba de aceptación de la rebanada de captura que las diez tareas siguientes
tienen que hacer pasar.

Este plan no entrega funcionalidad de negocio; entrega el **guardián** que hace que la arquitectura
hexagonal sea real en vez de decorativa (Criterio de Éxito 1) y el entorno de prueba que ya vive en
una ruta con espacios y acentos (Criterio de Éxito 6), de modo que DIS-06 sea una condición de todo
el entorno y no un caso especial que alguien rompa por descuido en la Fase 5.

Implementa D-32 (idioma del código), D-36 (identificador técnico `porteria`), D-50 y D-51 (CLI en
español con salida legible y estructurada bajo bandera), D-52 (entorno separado de aislamiento),
D-53 (compuerta rápida bloqueante, `lenta` en tanda programada), D-54 (Windows como única
plataforma de compuerta), D-55 (inventario de licencias desde el día uno), D-56 (prueba antes del
código para lo que la fase promete demostrar) y D-57 (cobertura medida, nunca compuerta).

Purpose: sin este plan, ninguna prueba de comportamiento de la fase puede siquiera ejecutarse, y
el criterio de éxito 1 no tiene instrumento.
Output: proyecto `uv` instalable, árbol de paquetes completo, cuatro compuertas ejecutables, la tabla de
invariantes de código multiplataforma y la prueba de aceptación de la rebanada en rojo declarado.

> **Este plan no es completamente autónomo.** La Tarea 4 es un `checkpoint:human-action` corto: configurar
> la protección de rama de `main`. Es configuración del repositorio, no un archivo, y es la mitad del
> Criterio de Éxito 1 que el código no puede entregar por sí solo.
</objective>

## Objetivo de la fase (verbatim de ROADMAP.md)

> Desde una orden por línea de comandos sobre un archivo de video, el sistema produce evidencia
> persistida con huella SHA-256 verificable, en una única transacción, sobre un esquema que ya modela
> viaje con varios remitos y peso teórico ausente; y los contratos de PALJET y de la balanza quedan
> congelados con dobles de prueba después de un GET real contra cada uno.

> **Nota de formato (MVP):** la línea `**Goal:**` de `ROADMAP.md` para la Fase 1 no está redactada en
> formato de historia de usuario (`As a … I want to … so that …`). Se transcribe verbatim en lugar de
> inventar una historia. Para convertirla, ejecutar `/gsd mvp-phase 1`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-RESEARCH.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-CONTEXT.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-VALIDATION.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/SKELETON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Proyecto uv, árbol de paquetes y las cuatro configuraciones de compuerta</name>
  <files>pyproject.toml, uv.lock, .python-version, .gitignore, src/porteria/**/__init__.py, src/porteria/infraestructura/transporte/ipc/README.md, src/porteria/cli/app.py, tests/recursos/videos/README.md, tests/__init__.py, tests/arquitectura/__init__.py, tests/arquitectura/invariantes/__init__.py, tests/aceptacion/__init__.py, tests/recursos/__init__.py, tests/dominio/__init__.py, tests/integracion/__init__.py, tests/contrato/__init__.py, tests/migracion/__init__.py, tests/lentas/__init__.py</files>
  <read_first>
    - `.planning/phases/01-.../01-RESEARCH.md` § "Standard Stack" (versiones exactas y licencias de las 15 dependencias verificadas)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Recommended Project Structure" (árbol de paquetes con los huecos vacíos obligatorios)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pattern 1: Prueba de arquitectura en dos compuertas" (los contratos de import-linter, ya escritos en TOML)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pitfall 8" (consola cp1252 y el modo UTF-8) y § "Pattern 9" (CLI en español, constantes de `typer.rich_utils`)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pitfall 10" (por qué la compuerta de licencias usa `--allow-only` con lista blanca y no `--fail-on` con lista negra)
    - `.planning/phases/01-.../01-CONTEXT.md` §Decisions D-32, D-36, D-49, D-50, D-51, D-52, D-53, D-55, D-57
    - `CLAUDE.md` § "Recommended Stack" y § "What NOT to Use"
  </read_first>
  <action>
Crear el proyecto con `uv init --package --name porteria` sobre layout `src/`, fijar `.python-version` en `3.12` y ejecutar `uv python pin 3.12`.

En `pyproject.toml`:

`[project]` — `name = "porteria"`, `requires-python = ">=3.12,<3.13"`, `dependencies` con las versiones exactas verificadas en RESEARCH § Standard Stack: `sqlalchemy==2.0.51`, `alembic==1.18.5`, `pydantic==2.13.4`, `pydantic-settings==2.14.2`, `opencv-python-headless==5.0.0.93`, `numpy==2.5.1`, `typer==0.27.0`, `argon2-cffi==25.1.0`, `structlog==26.1.0`, `platformdirs==4.11.0`. **Usar 1.18.5 de Alembic, no el 1.16.x que dice `CLAUDE.md`** — está desactualizado y RESEARCH lo verificó contra PyPI. **Prohibido** cualquier paquete de Ultralytics, YOLOv9, YOLOv7, YOLO-NAS, `PyQt6` y `opencv-python` no-headless.

**Decisión de transporte HTTP, tomada acá para que nadie la improvise en la ola 5.** El producto **no
agrega ninguna dependencia HTTP**. Las consultas a PALJET y a la balanza del plan 01-10 son GET simples
con autenticación básica y timeout, y se resuelven con `urllib.request` de la biblioteca estándar,
envuelto en `porteria.composicion.transporte_externo` e **inyectado** en los adaptadores. Tres razones,
en orden de peso: (a) el inventario de licencias de este plan y la auditoría de legitimidad de paquetes
de RESEARCH cubren exactamente 15 paquetes — agregar `httpx` arrastraría `httpcore`, `h11`, `anyio`,
`sniffio`, `certifi` e `idna`, seis paquetes sin auditar que obligarían a un checkpoint humano de
legitimidad y a reabrir la lista blanca de `pip-licenses`; (b) el instalador ya pesa 190–260 MB y
PyInstaller sumaría seis superficies más de import oculto; (c) el contrato `sin_conexion_cruda` prohíbe
`urllib` dentro de `infraestructura.externos`, y con el import viviendo en `composicion/` el contrato
sigue intacto **sin excepciones**. **Prohibido** agregar `requests`, `httpx`, `aiohttp` o `urllib3` a
`dependencies` en esta fase; si algún día PALJET exige certificado de cliente, pool de conexiones o
HTTP/2, el transporte es un objeto inyectado y cambiarlo es reescribir un módulo.

`[project.scripts]` — `porteria = "porteria.cli.app:main"`.

`[dependency-groups]` (PEP 735, no `optional-dependencies`) — grupo `dev` con `pytest==9.1.1`, `import-linter==2.13`, `ruff==0.16.0`, `pip-licenses==5.5.5`; y un grupo **`dominio`** que declara **únicamente** las dependencias que el paquete `dominio/` necesita, que a esta altura es el conjunto vacío. Ese grupo es lo que hace posible `uv sync --only-group dominio --no-default-groups --exact` (D-52).

`[tool.pytest.ini_options]` — `testpaths = ["tests"]`, `pythonpath = ["src"]`, `addopts = "-ra"`, y `markers = ["lenta: prueba de larga duración; tanda programada, no bloquea la fusión (D-53)"]`.

`[tool.importlinter]` — `root_package = "porteria"`, `include_external_packages = true`. **No activar `exclude_type_checking_imports`**: dejaría pasar los imports bajo `TYPE_CHECKING`, que es una de las cuatro vías de evasión que RESEARCH verificó. Cuatro contratos, copiados de RESEARCH § Pattern 1 y ampliados con el cuarto que exige la sección Security Domain:
  1. `dominio_limpio` — tipo `forbidden`, `source_modules = ["porteria.dominio"]`, `forbidden_modules` con `cv2`, `onnxruntime`, `PySide6`, `av`, `numpy`, `sqlalchemy`, `alembic`, `typer`, `click`, `structlog`, `argon2`, `pydantic`; `allow_indirect_imports = false`, `as_packages = true`.
  2. `capas` — tipo `layers`, `layers = ["porteria.cli", "porteria.infraestructura", "porteria.aplicacion", "porteria.dominio"]`, `exhaustive = false`.
  3. `sin_conexion_cruda` — tipo `forbidden`, `source_modules = ["porteria.infraestructura.externos"]`, `forbidden_modules = ["sqlite3", "requests", "urllib", "http.client", "socket"]`, `allow_indirect_imports = false`.
  4. `sin_deserializacion_insegura` — tipo `forbidden`, `source_modules = ["porteria"]`, `forbidden_modules = ["pickle", "shelve", "marshal"]`, `allow_indirect_imports = false`. Mitiga T-01-07.

`[tool.ruff]` — `line-length = 100`, `target-version = "py312"`, reglas `E,F,I,B,UP,S` y, para T-01-05/T-01-03, la regla que prohíbe SQL construido por interpolación (`S608`) habilitada sin excepciones en `src/porteria/infraestructura/`.

`[tool.coverage]` — activada y reportada, **sin `fail_under`** (D-57: la cobertura se mide y se ve; lo que bloquea es que existan y pasen las pruebas de los criterios de éxito).

Crear todo el árbol de paquetes de RESEARCH § Recommended Project Structure con `__init__.py` vacíos, **incluidos los huecos que deben quedar vacíos a propósito**: `src/porteria/infraestructura/transporte/ipc/README.md` con una línea explicando que el directorio existe como recordatorio de que el contrato núcleo↔UI es serializable y versionado desde el primer mensaje aunque el transporte de v1 sea en memoria. Crear todos los `__init__.py` **en esta tarea**, para que ningún plan posterior de la misma ola colisione creando el mismo archivo. **La misma regla vale para el árbol de pruebas**, y es igual de necesaria: crear también `tests/__init__.py` y los `__init__.py` de `tests/arquitectura/`, `tests/arquitectura/invariantes/`, `tests/aceptacion/`, `tests/recursos/`, `tests/dominio/`, `tests/integracion/`, `tests/contrato/`, `tests/migracion/` y `tests/lentas/` — los diez, aunque los módulos que van dentro los escriban planes posteriores. Sin esto, 01-02 y 01-03 (misma ola, sin dependencia entre sí) colisionan creando `tests/integracion/__init__.py`, y lo mismo pasa con `tests/dominio/` entre 01-02 y 01-09.

Crear `src/porteria/cli/app.py` con: (a) `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` y lo mismo para `sys.stderr` como **primeras sentencias del módulo**, antes de importar Typer — es la única vía que sobrevive al empaquetado con PyInstaller sin depender del entorno del cliente; (b) las constantes de `typer.rich_utils` traducidas (`OPTIONS_PANEL_TITLE = "Opciones"`, `COMMANDS_PANEL_TITLE = "Órdenes"`, `ARGUMENTS_PANEL_TITLE = "Argumentos"`, `ERRORS_PANEL_TITLE = "Error"`, `ABORTED_TEXT = "Cancelado."`, `REQUIRED_LONG_STRING = "[obligatorio]"`, `DEFAULT_STRING = "por defecto: {}"`); (c) `app = typer.Typer(add_completion=False, help="Herramienta de diagnóstico de portería.")`; (d) una función `_registrar_ordenes()` que recorra `pkgutil.iter_modules(porteria.cli.ordenes.__path__)`, importe cada módulo y llame a su función `registrar(app)`. **Ningún plan posterior edita `app.py`**: cada orden nueva es un módulo propio en `cli/ordenes/`. Los nombres de opción van en español **sin tildes** (`--camara`, no `--cámara`) porque el teclado y el encoding de la consola del cliente no están garantizados; las tildes son libres en la ayuda y en la salida.

Aceptar que los mensajes propios de Click (`No such command`, `Missing argument`) siguen en inglés; no se subclasea `TyperGroup` en esta fase — queda anotado como pendiente de la fase de interfaz.

`.gitignore` — `.venv/`, `__pycache__/`, `*.sqlite3`, `*.sqlite3-wal`, `*.sqlite3-shm`, `.pytest_cache/`, `.ruff_cache/`, `.import_linter_cache/`, y `tests/recursos/videos/*.mp4` (los recortes reales de D-59 se versionan explícitamente cuando existan; el sintético se genera en `conftest`). `tests/recursos/videos/README.md` explica esa política y cita D-58/D-59.

Ejecutar `uv sync` y versionar `uv.lock`.
  </action>
  <verify>
    <automated>uv sync && uv run python -c "import porteria; print(porteria.__name__)" && uv run lint-imports --no-cache && uv run porteria --help</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` termina con exit 0 y `uv.lock` existe y está versionado
    - `pyproject.toml` contiene `[tool.importlinter]`, `include_external_packages = true` y exactamente cuatro bloques `[[tool.importlinter.contracts]]` con `id` igual a `dominio_limpio`, `capas`, `sin_conexion_cruda` y `sin_deserializacion_insegura`
    - `pyproject.toml` contiene `markers = [` seguido de una entrada que empieza con `"lenta:`
    - `pyproject.toml` contiene `[dependency-groups]` con una clave `dominio`
    - `pyproject.toml` declara `alembic==1.18.5` (no 1.16.x)
    - `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` devuelve exit 0 y `tests/arquitectura/invariantes/inv_01_01.py` declara al menos estas invariantes: `pyproject.toml` sin `ultralytics`, sin `yolo`, sin `PyQt6`, sin `opencv-python==` (sólo la variante headless), sin `atomicwrites`, sin `requests` y sin `httpx`
    - `uv run lint-imports --no-cache` imprime `Contracts: 4 kept, 0 broken.` y devuelve exit 0
    - `uv run porteria --help` devuelve exit 0 y su salida contiene la cadena `Órdenes` y no lanza `UnicodeEncodeError`
    - Invariante declarada en `tests/arquitectura/invariantes/inv_01_01.py`: `src/porteria/cli/app.py` contiene `reconfigure(encoding="utf-8"` dentro de las primeras 10 líneas del archivo
    - `src/porteria/infraestructura/transporte/ipc/README.md` existe y no hay ningún `.py` en ese directorio
    - `uv sync --only-group dominio --no-default-groups --exact` termina con exit 0
    - Existen los diez `__init__.py` del árbol de pruebas (`tests/`, `tests/arquitectura/`, `tests/arquitectura/invariantes/`, `tests/aceptacion/`, `tests/recursos/`, `tests/dominio/`, `tests/integracion/`, `tests/contrato/`, `tests/migracion/`, `tests/lentas/`), creados por este plan y por ninguno posterior
    - `pyproject.toml` **no** declara ninguna dependencia HTTP (`requests`, `httpx`, `aiohttp`, `urllib3`): el transporte del plan 01-10 es `urllib.request` de la stdlib inyectado desde `composicion/`
  </acceptance_criteria>
  <done>El proyecto instala, la CLI arranca en español sin romper el encoding de la consola de Windows, el árbol de paquetes completo existe y los cuatro contratos de arquitectura se evalúan en verde sobre un árbol vacío.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Fixtures de ruta hostil, sonda de aislamiento del dominio y prueba de aceptación de la rebanada</name>
  <files>tests/conftest.py, tests/arquitectura/sonda_dominio_aislado.py, tests/arquitectura/test_dominio_aislado.py, tests/aceptacion/test_rebanada_captura.py</files>
  <read_first>
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pattern 1" (la sonda de aislamiento verificada, con `pkgutil.walk_packages` y el conjunto `PROHIBIDOS`)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pitfall 3" (MAX_PATH activo: la raíz de evidencia no puede pasar de ~170 caracteres; el sufijo `AAAA/MM/DD/<64hex>.jpg` mide 89)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pattern 7" (reloj: `perf_counter_ns` para deltas, ancla UTC)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Validation Architecture" → "Criterio 6 — rutas raras" (la fixture de ruta rara se aplica a toda la suite, no a un test aislado)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Open Questions" 5 (el video sintético con `cv2.VideoWriter` alcanza para medir el contrato de frescura)
    - `.planning/phases/01-.../01-VALIDATION.md` § "Wave 0 Requirements" (la lista completa que esta tarea tiene que dejar instalada)
    - `.planning/phases/01-.../SKELETON.md` § "Capacidad probada de punta a punta" (el resultado observable exacto que la prueba de aceptación describe)
  </read_first>
  <behavior>
    - `ruta_hostil` (fixture de sesión, `autouse=True`): **no usa `tmp_path_factory`**. La base de `tmp_path_factory` en Windows (`C:\Users\<usuario>\AppData\Local\Temp\pytest-of-<usuario>\pytest-N\...`) más `IA visual ñandú áéí\evidencia fotográfica` queda pegada al límite de 170 caracteres y a menudo por encima. La raíz se ancla en una base **corta y controlada**, resuelta en `pytest_sessionstart` recorriendo estos candidatos en orden: (1) la variable de entorno `PORTERIA_RAIZ_PRUEBAS` si está definida; (2) `<anchor de la unidad actual>p` (por ejemplo `C:\p`); (3) `%LOCALAPPDATA%\pp` en Windows o `~/.pp` en POSIX. Bajo la base elegida crea `IA visual ñandú áéí` con los subdirectorios `base de datos` y `evidencia fotográfica`, y borra ese subárbol en el teardown de sesión.
    - Si **ningún** candidato es creable, o si el largo absoluto de la raíz de evidencia resultante supera **170 caracteres**, `pytest_sessionstart` levanta `pytest.UsageError` con un mensaje que dice el máximo, el largo obtenido y cómo fijar `PORTERIA_RAIZ_PRUEBAS`. **Prohibido `pytest.skip`**: un skip de alcance de sesión hace que `uv run pytest -q -m "not lenta"` termine con exit 0 sin ejecutar nada y que `scripts/compuerta.py` propague ese 0 como compuerta verde — exactamente el fallo silencioso que el resto de la fase se esfuerza en cerrar. Fallar es la única opción honesta.
    - Hook `pytest_sessionfinish` en el mismo `conftest.py`: si `session.testscollected == 0`, imprime en español que no se recolectó ninguna prueba y fuerza un `session.exitstatus` distinto de 0. `--exitfirst` no cubre este caso, porque el problema no es una prueba que falla sino que no hubo nada que ejecutar.
    - `motor_sqlite` (fixture de función): devuelve un Engine sobre `<ruta_hostil>/base de datos/porteria.sqlite3`. En este plan la fixture aún no puede importar `crear_motor` (llega en 01-05): se implementa con `sqlalchemy.create_engine(URL.create("sqlite", database=...))` y se marca con un comentario `# 01-05 reemplaza esta construcción por porteria.infraestructura.persistencia.sqlite.motor.crear_motor`.
    - `reloj_determinista` (fixture de función): un doble que devuelve instantes monotónicos controlados por el test, para que ninguna prueba de dominio dependa del reloj real.
    - `video_sintetico` (fixture de sesión): genera con `cv2.VideoWriter` un archivo de 320×240 a 25 fps y ~30 s bajo `<ruta_hostil>` y devuelve su `Path`. Se genera, no se versiona (D-59).
    - `sonda_dominio_aislado.py`: su **primera sentencia ejecutable**, antes de cualquier import del proyecto, es `sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src"))`. Con `--no-project` el paquete `porteria` no está instalado, así que sin esa línea `import porteria.dominio` falla con `ModuleNotFoundError` y la compuerta mide otra cosa. Auto-resolverse es lo que hace que **el mismo comando literal** funcione a mano, desde pytest y desde `scripts/compuerta.py`, sin depender de `PYTHONPATH`. Después importa `porteria.dominio`, recorre `pkgutil.walk_packages` importando **todos** los submódulos (no sólo el paquete raíz), y sale con 1 si alguno de `{cv2, onnxruntime, PySide6, numpy, sqlalchemy, alembic, av, typer, structlog, argon2, pydantic}` quedó en `sys.modules`. Imprime la lista de los que quedaron y la cantidad de submódulos importados.
    - `test_dominio_aislado.py`: lanza esa sonda como subproceso con exactamente `uv run --python 3.12 --isolated --no-project python tests/arquitectura/sonda_dominio_aislado.py`, con `cwd` en la raíz del repositorio y **sin definir `PYTHONPATH`** — el mismo comando literal que el `must_have` y que el paso 3 de `scripts/compuerta.py` — y afirma exit 0. Afirma además, como anti-vacuidad, que la salida de la sonda declara haber importado al menos 1 submódulo.
    - `test_rebanada_captura.py`: marcada `@pytest.mark.xfail(reason="la rebanada cierra en el plan 01-07", strict=True)`. Invoca la CLI real (`typer.testing.CliRunner` sobre `porteria.cli.app.app`) con `capturar --fuente <video_sintetico>` y afirma: exit 0; exactamente 1 archivo bajo `<raiz_evidencia>/AAAA/MM/DD/`; el nombre del archivo es 64 caracteres hexadecimales + `.jpg`; recalcular SHA-256 del archivo reproduce ese nombre; exactamente 1 fila en `item_evidencia`; la columna `ruta_relativa` de esa fila es relativa, usa `/` como separador y apunta al archivo existente; `sha256` coincide.
  </behavior>
  <action>
Escribir `tests/conftest.py` con las cuatro fixtures descritas arriba. La de ruta hostil va con
`scope="session", autouse=True` porque el Criterio de Éxito 6 exige que DIS-06 sea una condición de
**todo** el entorno de prueba y no un caso especial: si la ruta rara vive sólo en un test, alguien la
rompe por descuido en la Fase 5 y nadie se entera. La fixture expone `raiz_evidencia` y `ruta_db`
como atributos para que los tests posteriores no reconstruyan rutas a mano.

Extraer la resolución de la base a una función propia —`resolver_base_de_pruebas() -> Path`— e invocarla
desde `pytest_sessionstart`, no desde la fixture: así se puede probar con una base artificialmente larga
y exigir que levante en vez de saltar. Que el error sea `pytest.UsageError` y no un `skip` es el punto
central de esta tarea: **una compuerta que pasa porque no corrió nada es peor que ninguna compuerta**, y
el par «skip de sesión + exit 0 propagado por `compuerta.py`» es la forma más silenciosa que tiene esta
fase de mentirse. Por eso el `conftest.py` lleva además el `pytest_sessionfinish` que falla con cero
pruebas recolectadas, y `scripts/compuerta.py` replica la misma comprobación en su paso 1.

El workflow de integración continua fija `PORTERIA_RAIZ_PRUEBAS=C:\p` de forma explícita, para que en CI
la base no dependa de la heurística de candidatos.

Escribir `tests/arquitectura/sonda_dominio_aislado.py` copiando la sonda verificada de RESEARCH
§ Pattern 1 y ampliando el conjunto `PROHIBIDOS` con `typer`, `structlog`, `argon2` y `pydantic`
para que coincida con `forbidden_modules` del contrato `dominio_limpio`. La sonda es un script, no un
test: se ejecuta con un intérprete que no tiene ninguno de esos paquetes instalados, así que un import
prohibido reventaría con `ModuleNotFoundError` antes de llegar a la comprobación —y ése es
exactamente el resultado que la prueba busca.

Escribir `tests/arquitectura/test_dominio_aislado.py` que lanza la sonda con
`uv run --python 3.12 --isolated --no-project python tests/arquitectura/sonda_dominio_aislado.py`
por `subprocess.run`, con `cwd` en la raíz del repositorio y **sin tocar `PYTHONPATH`**: la sonda se
auto-resuelve `src/` en `sys.path[0]` (ver `<behavior>`), y ésa es la única forma de que el comando del
`must_have`, el de esta prueba y el del paso 3 de `scripts/compuerta.py` sean **literalmente el mismo** y
puedan ser verdaderos a la vez. Afirmar exit 0 e incluir la salida completa del subproceso en el mensaje
del assert, para que un fallo en CI diga qué módulo se coló.

Escribir `tests/aceptacion/test_rebanada_captura.py` según el bloque `<behavior>`. Marcarla
`xfail(strict=True)`: mientras la rebanada no exista debe estar en rojo declarado (D-56, prueba antes
del código), y `strict=True` garantiza que si algún día pasara sin que nadie le saque el marcador, la
suite **falla** — que es la señal correcta. El plan 01-07 quita el marcador como parte de su cierre.

Todos los `assert` llevan mensaje en español claro indicando qué revisar (UI-05 se aplica también a
las pruebas: en una planta sin área de sistemas la salida de la compuerta es lo que se lee por
teléfono).
  </action>
  <verify>
    <automated>uv run pytest tests/arquitectura -q && uv run pytest tests/aceptacion -q -rx</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/arquitectura -q` devuelve exit 0
    - `uv run --python 3.12 --isolated --no-project python tests/arquitectura/sonda_dominio_aislado.py` devuelve exit 0 **ejecutado a mano** desde la raíz del repositorio, en un shell donde `PYTHONPATH` no está definido
    - `tests/arquitectura/sonda_dominio_aislado.py` inserta `src` en `sys.path` como primera sentencia ejecutable, antes de cualquier import del proyecto
    - `uv run pytest tests/aceptacion -q -rx` devuelve exit 0 y su resumen reporta la prueba como `xfailed` (no `xpassed`, no `failed`)
    - `tests/conftest.py` contiene la cadena `ñandú` y la cadena `autouse=True`
    - `tests/conftest.py` **no** usa `tmp_path_factory` para la raíz hostil y **no** contiene ningún `pytest.skip` en la resolución de la ruta
    - Existe una prueba que invoca `resolver_base_de_pruebas` con una base artificialmente larga y afirma que levanta `pytest.UsageError` con un mensaje que contiene el número del máximo (170) y el largo obtenido
    - `tests/conftest.py` define `pytest_sessionfinish`, y existe una prueba que afirma que con `testscollected == 0` el exit status resultante es distinto de 0
    - `tests/conftest.py` define las cuatro fixtures `ruta_hostil`, `motor_sqlite`, `reloj_determinista` y `video_sintetico`
    - `tests/arquitectura/sonda_dominio_aislado.py` contiene `walk_packages` y su conjunto de prohibidos incluye `cv2`, `onnxruntime`, `PySide6` y `numpy`
    - `tests/aceptacion/test_rebanada_captura.py` contiene `strict=True` y afirma sobre `item_evidencia`, sobre `sha256` y sobre `ruta_relativa`
    - El video sintético se genera bajo la ruta con acentos y `cv2.VideoCapture` lo abre con backend `FFMPEG`
  </acceptance_criteria>
  <done>Toda la suite corre desde una ruta con espacios y acentos; la sonda de aislamiento del dominio funciona contra un intérprete sin terceros; y la prueba de aceptación de la rebanada existe en rojo declarado con el resultado observable completo escrito.</done>
</task>

<task type="auto">
  <name>Tarea 3: Prueba de la prueba (sabotaje `import cv2`) y compuerta de CI en Windows</name>
  <files>tests/arquitectura/test_sabotaje_compuerta_dominio.py, tests/arquitectura/test_invariantes_de_codigo.py, tests/arquitectura/invariantes/inv_01_01.py, scripts/compuerta.py, .github/workflows/compuerta.yml</files>
  <read_first>
    - `.planning/phases/01-.../01-VALIDATION.md` § "Pruebas de la prueba (anti-vacuidad)" — el punto NUC-01 es esta tarea
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pattern 1" tabla "Vía de evasión" (las cuatro vías y la salida exacta que produce `import-linter`)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pitfall 10" y § "Package Legitimacy Audit" (la lista real de variantes de cadena de licencia observadas en las 35 dependencias transitivas)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Pitfall 11" (la suite de arquitectura que corre en menos de 50 ms es señal de que no ejercita nada)
    - `.planning/phases/01-.../01-CONTEXT.md` §Decisions D-53, D-54, D-55
    - `pyproject.toml` (los cuatro contratos que esta tarea somete a sabotaje)
  </read_first>
  <action>
Escribir `tests/arquitectura/test_sabotaje_compuerta_dominio.py`. La prueba escribe temporalmente un
módulo `src/porteria/dominio/comun/_sabotaje_temporal.py` con un `import cv2` **diferido dentro de una
función** —la vía de evasión más difícil de las cuatro—, ejecuta `uv run lint-imports --no-cache` por
subproceso y afirma tres cosas: exit code **1**; la salida menciona el módulo ofensor
(`porteria.dominio.comun._sabotaje_temporal`); y la salida menciona `cv2`. Borra el módulo en un
`finally` y, para que un fallo del test no deje el árbol contaminado, verifica en el `finally` que el
archivo ya no existe. Repite el ciclo con una segunda variante: `if TYPE_CHECKING: import cv2`.

Sin esta prueba, un contrato mal escrito pasa siempre y nadie se entera: es la diferencia entre tener
una compuerta y creer que se tiene una.

Escribir `tests/arquitectura/invariantes/inv_01_01.py` y `tests/arquitectura/test_invariantes_de_codigo.py`.
Esta maquinaria **reemplaza los criterios de aceptación basados en `grep` de los diez planes de la fase**,
y no por prolijidad: la plataforma de compuerta es **Windows únicamente** (D-54) y ni el shell del runner
ni el del cliente garantizan `grep`. Varios de esos criterios definen **propiedades de seguridad** —cero
`unlink`/`rmtree`/`remove` en el módulo de cuarentena, cero `pickle` en el grabador, cero `permisos` en
`cli/ordenes/usuarios.py`—, y cuando el comando no existe, «no se pudo verificar» se lee como «pasó».
Convertidas en aserciones de pytest corren dentro del paso 1 de `scripts/compuerta.py` en cualquier
plataforma y quedan como **regresión permanente** en vez de una comprobación manual de una sola vez.

Forma concreta:
  - `invariantes/inv_01_01.py` expone `INVARIANTES: tuple[Invariante, ...]`. `Invariante` lleva `ruta`
    (relativa a la raíz del repositorio), `modo` (`"presente"` o `"ausente"`), `patron` (expresión
    regular), `motivo` (texto en español que se imprime al fallar), `ignorar_comentarios` (por defecto
    `True`: se descartan las líneas que empiezan por `#` antes de buscar, porque la prosa de un
    encabezado que explica por qué algo no debe existir invalidaría la propia invariante) y
    `solo_primeras_lineas` (opcional, para el caso del `reconfigure` del punto de entrada).
  - `test_invariantes_de_codigo.py` descubre **todos** los módulos de `tests/arquitectura/invariantes/`
    con `pkgutil.iter_modules`, concatena sus `INVARIANTES` y parametriza una prueba por entrada. Cada
    plan posterior de la fase agrega su propio `inv_01_0N.py` y nada más: así ningún plan de la misma ola
    edita el archivo de otro.
  - Dos anti-vacuidades: falla si el conjunto concatenado de invariantes está **vacío**, y falla
    nombrando el archivo si una invariante declara una `ruta` que no existe en el árbol —una entrada sin
    archivo significa que el plan que la declaró no creó el módulo.

Las invariantes de **este** plan: `pyproject.toml` sin `ultralytics`, sin `yolo`, sin `PyQt6`, sin
`opencv-python==`, sin `atomicwrites`, sin `requests` y sin `httpx`; `src/porteria/cli/app.py` con
`reconfigure(encoding="utf-8"` dentro de las primeras 10 líneas; `scripts/compuerta.py` con
`--allow-only` y sin `--fail-on`.

Escribir `scripts/compuerta.py`: un script Python sin dependencias fuera de la stdlib que ejecuta en
orden los cuatro pasos, imprime un encabezado en español por cada uno, **no corta al primer fallo**
(corre los cuatro para que un solo empujón muestre todos los problemas) y sale con el **peor** exit
code de los cuatro:
  1. `uv run pytest -q -m "not lenta"` — y **tratar el exit code 5 de pytest («no tests ran») como
     fallo**, además de comprobar que la cantidad de pruebas recolectadas que reporta la salida es mayor
     que cero. Es la misma comprobación que el `pytest_sessionfinish` del `conftest.py`, replicada acá
     porque la compuerta no puede confiar en que el `conftest` se haya cargado.
  2. `uv run lint-imports --no-cache`
  3. `uv run --python 3.12 --isolated --no-project python tests/arquitectura/sonda_dominio_aislado.py`
  4. `uv run pip-licenses --allow-only "<lista blanca>"` — **lista blanca, no lista negra**: D-55 exige
     fallar ante una licencia contagiosa **o no identificable**, y `--fail-on` con lista negra pasa en
     verde ante una cadena desconocida. La lista blanca son las variantes observadas en las 35
     dependencias transitivas del inventario verificado: `MIT`, `MIT License`, `MIT-0`,
     `MIT OR Apache-2.0`, `MIT AND PSF-2.0`, `BSD-3-Clause`, `BSD-2-Clause`, `BSD License`,
     `Apache-2.0`, `Apache Software License`, `Apache-2.0 OR BSD-2-Clause`, `ISC`, `PSF-2.0`,
     `BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0`. Agregar además una comprobación propia que
     falle si alguna dependencia reporta licencia `UNKNOWN`.
El script acepta `--json` para emitir el resultado estructurado (D-51) además del texto legible.

Escribir `.github/workflows/compuerta.yml` que corre sobre **`windows-latest` y sólo sobre
`windows-latest`** (D-54), instala `uv` con `astral-sh/setup-uv`, hace `uv sync`, y ejecuta
`uv run python scripts/compuerta.py`. Un segundo job `tanda-lenta` con `schedule: cron` diario ejecuta
`uv run pytest -q -m lenta` y **no** es requerido para la fusión (D-53): una compuerta lenta es una
compuerta que se termina salteando.

El workflow define la variable de entorno `PYTHONUTF8=1` como cinturón adicional al
`reconfigure` del punto de entrada, y `PORTERIA_RAIZ_PRUEBAS=C:\p` para que la base de la ruta hostil
en CI sea explícita en vez de depender de la heurística de candidatos del `conftest.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/arquitectura/test_sabotaje_compuerta_dominio.py -q && uv run python scripts/compuerta.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/arquitectura/test_sabotaje_compuerta_dominio.py -q` devuelve exit 0
    - Tras correr la prueba, `src/porteria/dominio/comun/_sabotaje_temporal.py` no existe
    - `uv run python scripts/compuerta.py` devuelve exit 0 sobre el árbol limpio y su salida nombra los cuatro pasos
    - Introducir a mano `import cv2` en cualquier módulo de `src/porteria/dominio/` hace que `uv run python scripts/compuerta.py` devuelva un exit code distinto de 0
    - `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` devuelve exit 0 y las invariantes de este plan sobre `scripts/compuerta.py` (presencia de `--allow-only`, ausencia de `--fail-on`) están declaradas en `tests/arquitectura/invariantes/inv_01_01.py`
    - `tests/arquitectura/test_invariantes_de_codigo.py` descubre los módulos de `tests/arquitectura/invariantes/` con `pkgutil.iter_modules`, **falla** si el conjunto concatenado de invariantes está vacío, y **falla nombrando el archivo** si una invariante declara una ruta que no existe
    - `scripts/compuerta.py` corre los cuatro pasos aunque el primero falle (verificable: sabotear el paso 1 y comprobar que la salida sigue nombrando los pasos 2, 3 y 4)
    - Sabotear la recolección (por ejemplo con un `conftest.py` que no encuentre su base de pruebas, o corriendo `uv run pytest -q -m "marcador-inexistente"`) hace que `scripts/compuerta.py` devuelva un exit code distinto de 0 en lugar de reportar el paso 1 en verde
    - `.github/workflows/compuerta.yml` contiene `windows-latest` y no contiene `ubuntu-` ni `macos-`
    - `.github/workflows/compuerta.yml` define un job con `schedule:` que ejecuta `-m lenta` y no está listado como required
  </acceptance_criteria>
  <done>La compuerta de arquitectura está demostrada, no supuesta: existe una prueba que la sabotea a propósito y exige que falle. La compuerta completa corre en Windows con un solo comando y propaga el peor resultado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Tarea 4: Protección de rama — que la compuerta en rojo bloquee la fusión</name>
  <files>(ninguno: es configuración del repositorio en GitHub, no un archivo del árbol)</files>
  <read_first>
    - `.planning/phases/01-.../01-CONTEXT.md` §Decisions D-53 (compuerta rápida bloqueante, `lenta` en tanda programada), D-54 (Windows como única plataforma de compuerta)
    - `.github/workflows/compuerta.yml` y `scripts/compuerta.py` (creados en la Tarea 3)
  </read_first>
  <what-built>
Claude automatizó todo lo automatizable: `scripts/compuerta.py` corre las cuatro compuertas y propaga el
peor exit code, `.github/workflows/compuerta.yml` lo ejecuta sobre `windows-latest` en cada empujón, y un
segundo job `tanda-lenta` corre `-m lenta` por agenda diaria.

Lo que Claude **no puede hacer** es configurar la protección de rama: es un ajuste del repositorio en la
interfaz de GitHub, no un archivo versionado. Y sin él el Criterio de Éxito 1 queda a medias. El criterio
dice que ante una dependencia prohibida «la prueba falla y **no se puede fusionar**»: son dos
afirmaciones, y sólo la primera es código. Por ser configuración y no archivo, es justo lo que se olvida.
  </what-built>
  <how-to-verify>
1. En GitHub: `Settings → Branches → Add branch protection rule` para `main`.
2. Activar `Require status checks to pass before merging` y seleccionar el check del job de
   `compuerta.yml` que ejecuta `uv run python scripts/compuerta.py`.
3. **No** seleccionar el job `tanda-lenta`: D-53 es explícito en que la tanda lenta abre un asunto al
   fallar y no traba la fusión. Una compuerta lenta obligatoria es una compuerta que se termina salteando.
4. Comprobarlo de verdad, no de palabra: crear una rama que introduzca `import cv2` en cualquier módulo de
   `src/porteria/dominio/`, abrir el pull request y verificar que el botón de fusión queda bloqueado por el
   check en rojo. Cerrar la rama sin fusionar.
5. Pegar en `01-01-SUMMARY.md` el nombre exacto del check requerido, la lista de checks requeridos (para
   que se vea que `tanda-lenta` no está) y el resultado del paso 4.

Si el repositorio todavía no está en GitHub, o el plan del repositorio no permite protección de rama,
anotarlo en el SUMMARY como **deuda declarada** con el mecanismo alternativo que se va a usar (por ejemplo
un hook `pre-push` que corra `scripts/compuerta.py`) — pero no darlo por cumplido en silencio.
  </how-to-verify>
  <acceptance_criteria>
    - `main` tiene una regla de protección que exige el check de `compuerta.yml` para poder fusionar
    - El job `tanda-lenta` **no** figura entre los checks requeridos
    - `01-01-SUMMARY.md` contiene la evidencia del paso 4: un pull request con una dependencia prohibida en `dominio/` que no se pudo fusionar
    - Si no fue posible configurarlo, `01-01-SUMMARY.md` lo registra como deuda declarada con el mecanismo alternativo y el motivo
  </acceptance_criteria>
  <resume-signal>Escribir "protección configurada" indicando el nombre del check requerido, o "no aplicable" con el mecanismo alternativo y el motivo.</resume-signal>
  <done>El Criterio de Éxito 1 está completo en sus dos mitades: la compuerta falla cuando debe fallar, y cuando falla no se puede fusionar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositorio → entorno de ejecución | El `pyproject.toml` y el lockfile determinan qué código de terceros corre en la máquina del desarrollador y del cliente |
| árbol de código → paquete `dominio/` | La frontera arquitectónica que separa lo que no se puede reescribir barato de lo que sí |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering / RCE | todo `porteria` | Media | mitigate | Contrato `sin_deserializacion_insegura` de import-linter prohíbe `pickle`, `shelve` y `marshal` en todo el paquete (Tarea 1). Verificable: `uv run lint-imports --no-cache` devuelve 1 si alguien los importa |
| T-01-05 | Tampering | `infraestructura/persistencia` | Baja | mitigate | Regla Ruff `S608` habilitada sin excepciones sobre `src/porteria/infraestructura/` para prohibir SQL construido por interpolación de cadenas (Tarea 1) |
| T-01-SC | Tampering | instalación de paquetes PyPI | Media | mitigate | `01-RESEARCH.md` § Package Legitimacy Audit ya ejecutó el protocolo sobre los 15 paquetes: **cero `[SLOP]`, cero `[SUS]` reales** (los 15 `SUS` del seam son falsos positivos sistemáticos por `unknown-downloads` y por `too-new` calculado sobre el último release). Aprobación sostenida por primera publicación + número de releases + repositorio identificado + instalación real + inventario de licencias del cierre transitivo. Mitigación operativa: versiones exactas pinneadas, `uv.lock` determinista versionado, y compuerta `pip-licenses --allow-only` con lista blanca (Tarea 3). **No se requiere checkpoint humano de legitimidad** |
| T-01-11 | Tampering | compuerta de arquitectura | Media | mitigate | Una compuerta que nunca falló no está demostrada. Tarea 3 la sabotea a propósito con las dos vías de evasión más difíciles (import diferido en función, import bajo `TYPE_CHECKING`) y exige exit 1 con el módulo ofensor nombrado |
| T-01-12 | Information Disclosure | inventario de licencias | Baja | mitigate | Lista **blanca** en vez de lista negra: falla ante lo desconocido, que es lo que D-55 pide ("contagiosa **o no identificable**"). Complementada por una comprobación propia que falla ante licencia `UNKNOWN` |
</threat_model>

## Artifacts this phase produces (plan 01-01)

**Archivos de configuración:** `pyproject.toml`, `uv.lock`, `.python-version`, `.gitignore`, `.github/workflows/compuerta.yml`

**Símbolos y claves de configuración creados:**
- `[project.scripts]` → `porteria`
- `[tool.pytest.ini_options]` → `testpaths`, `pythonpath`, `addopts`, `markers` (marcador `lenta`)
- `[tool.importlinter]` → `root_package`, `include_external_packages`
- Contratos de import-linter: `dominio_limpio`, `capas`, `sin_conexion_cruda`, `sin_deserializacion_insegura`
- `[dependency-groups]` → grupos `dev` y `dominio`
- `[tool.ruff]`, `[tool.coverage]`

**Módulos Python creados:**
- `porteria` (paquete raíz) y todo el árbol de subpaquetes vacíos listado en `files_modified`
- `porteria.cli.app` → `app` (instancia `typer.Typer`), `main()`, `_registrar_ordenes()`
- `porteria.cli.ordenes` (paquete de descubrimiento; contrato: cada módulo expone `registrar(app)`)

**Scripts:** `scripts/compuerta.py` (acepta `--json`)

**Pruebas y fixtures creadas:**
- `tests/conftest.py` → fixtures `ruta_hostil` (sesión, autouse), `motor_sqlite`, `reloj_determinista`, `video_sintetico`
- `tests/conftest.py` → función `resolver_base_de_pruebas()` y hooks `pytest_sessionstart` / `pytest_sessionfinish`
- `tests/arquitectura/sonda_dominio_aislado.py` → constante `PROHIBIDOS` (auto-resuelve `src/` en `sys.path[0]`)
- `tests/arquitectura/test_dominio_aislado.py`, `tests/arquitectura/test_sabotaje_compuerta_dominio.py`
- `tests/arquitectura/invariantes/` → paquete descubierto con `pkgutil.iter_modules`; contrato: cada módulo `inv_01_0N.py` expone `INVARIANTES: tuple[Invariante, ...]`
- `tests/arquitectura/test_invariantes_de_codigo.py` → tipo `Invariante` (`ruta`, `modo`, `patron`, `motivo`, `ignorar_comentarios`, `solo_primeras_lineas`)
- Los diez `__init__.py` del árbol de pruebas (`tests/`, `arquitectura/`, `arquitectura/invariantes/`, `aceptacion/`, `recursos/`, `dominio/`, `integracion/`, `contrato/`, `migracion/`, `lentas/`)
- `tests/aceptacion/test_rebanada_captura.py`
- `tests/recursos/videos/README.md`

**Rutas nuevas:** `src/porteria/`, `tests/`, `scripts/`, `.github/workflows/`, `src/porteria/infraestructura/transporte/ipc/` (deliberadamente sin `.py`)

**Variable de entorno nueva:** `PORTERIA_RAIZ_PRUEBAS` — base corta para la ruta hostil de la suite; en CI se fija en `C:\p`

**Decisión de dependencias fijada acá para toda la fase:** el transporte HTTP de los adaptadores externos (plan 01-10) es `urllib.request` de la biblioteca estándar, inyectado desde `porteria.composicion.transporte_externo`. El producto **no** agrega `requests`, `httpx`, `aiohttp` ni `urllib3`

<verification>
- `uv run python scripts/compuerta.py` devuelve 0 sobre el árbol limpio
- `uv run lint-imports --no-cache` reporta `Contracts: 4 kept, 0 broken.`
- `uv run --isolated --no-project python tests/arquitectura/sonda_dominio_aislado.py` devuelve 0
- `uv run pytest -q -m "not lenta"` devuelve 0 con la prueba de aceptación reportada como `xfailed` y con una cantidad de pruebas recolectadas mayor que cero
- `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` devuelve 0 y la tabla de invariantes no está vacía
- Toda la suite escribe bajo una ruta que contiene espacios y `ñandú áéí`, anclada en una base corta y controlada
</verification>

<success_criteria>
- El Criterio de Éxito 1 tiene instrumento ejecutable y **demostrado** en sus dos mitades: la compuerta falla cuando debe fallar, y cuando falla la fusión queda bloqueada (protección de rama configurada y comprobada con un pull request real)
- Ninguna compuerta de la fase puede pasar en verde por no haber ejecutado nada: la resolución de la ruta hostil falla en vez de saltar, y tanto el `conftest` como `scripts/compuerta.py` tratan «0 pruebas recolectadas» como fallo
- El Criterio de Éxito 6 tiene su mitad de "rutas raras" instalada como condición de todo el entorno de prueba
- La prueba de aceptación de la rebanada existe, en rojo declarado, antes de que exista una línea de código de negocio (D-56)
- Ningún plan posterior necesita editar `pyproject.toml` ni `cli/app.py` para agregar una orden
</success_criteria>

<output>
Crear `.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-01-SUMMARY.md` al terminar.
</output>
