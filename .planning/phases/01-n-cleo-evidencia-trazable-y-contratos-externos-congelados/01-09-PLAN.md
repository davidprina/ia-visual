---
phase: 01-n-cleo-evidencia-trazable-y-contratos-externos-congelados
plan: 09
type: execute
wave: 5
depends_on: [01-05, 01-07]
files_modified:
  - src/porteria/dominio/identidad/modelo.py
  - src/porteria/dominio/identidad/permisos.py
  - src/porteria/dominio/identidad/politica.py
  - src/porteria/aplicacion/puertos/salida/repositorio_usuarios.py
  - src/porteria/aplicacion/puertos/salida/hasheador.py
  - src/porteria/aplicacion/casos_de_uso/gestionar_identidad.py
  - src/porteria/aplicacion/servicios/sesion.py
  - src/porteria/infraestructura/seguridad/argon2.py
  - src/porteria/infraestructura/persistencia/sqlite/repositorio_usuarios.py
  - src/porteria/cli/ordenes/crear_administrador.py
  - src/porteria/cli/ordenes/usuarios.py
  - tests/arquitectura/invariantes/inv_01_09.py
  - tests/dominio/test_identidad.py
  - tests/dominio/test_permisos.py
  - tests/integracion/test_credenciales.py
  - tests/integracion/test_autoria_en_evidencia.py
autonomous: true
requirements: [NUC-05]
must_haves:
  truths:
    - "No existe ninguna credencial por defecto en el producto: un asistente de primer arranque crea el administrador inicial"
    - "Las credenciales se protegen con Argon2id con sal por usuario y formato PHC, y el hash nunca aparece en la bitácora ni en ninguna salida"
    - "Los cuatro roles del negocio —Portero, Logística, Compras y Administración— existen en el esquema y sus permisos están definidos en código"
    - "Un usuario que ya firmó evidencia no se elimina: se desactiva, no puede volver a iniciar sesión y todas sus firmas conservan su nombre"
    - "La captura nunca se bloquea por falta de sesión: sin sesión se registra con autor no identificado y el hecho queda marcado"
    - "El actor de cada registro de la bitácora encadenada y la columna de autor de toda fila auditable quedan pobladas dentro de la misma transacción única"
    - "**Alcance declarado de la Fase 1 (no es capacidad entregada al operador):** el camino *con sesión iniciada* se ejercita **sólo por la API de aplicación** —`Sesion.iniciar` + `CasoDeUsoEjecutarCaptura`— en pruebas de integración. Ninguna orden de consola de esta fase inicia sesión ni acepta `--operador`/`--autor` en `porteria capturar`, y este plan no toca `capturar.py` ni `arranque.py`. Desde el producto, el camino observable de la Fase 1 es el de **autor no identificado**. La superficie de operador llega con la pantalla de inicio de sesión, diferida a la fase de interfaz. La verificación de fase no debe leer la autoría con sesión como funcionalidad disponible para el portero"
    - "Las propiedades de código de este plan —sin borrado de usuarios, sin expiración de sesión, sin decisión de permisos en la consola, sin la API de versión deprecada— se afirman con invariantes de Python que corren en Windows, no con comandos de shell (D-54)"
  artifacts:
    - path: "src/porteria/dominio/identidad/modelo.py"
      provides: "Usuario y Rol con los cuatro roles fijos"
      contains: "class Usuario"
    - path: "src/porteria/dominio/identidad/permisos.py"
      provides: "Matriz de permisos en código, aislada y con pruebas propias"
    - path: "src/porteria/infraestructura/seguridad/argon2.py"
      provides: "Hasheo Argon2id con formato PHC y check_needs_rehash"
      contains: "argon2"
    - path: "src/porteria/cli/ordenes/crear_administrador.py"
      provides: "Asistente de primer arranque: crea el administrador inicial"
    - path: "tests/arquitectura/invariantes/inv_01_09.py"
      provides: "Los cinco invariantes de código de este plan, descubiertos por tests/arquitectura/test_invariantes_de_codigo.py (plan 01-01)"
      contains: "INVARIANTES"
  key_links:
    - from: "src/porteria/infraestructura/seguridad/argon2.py"
      to: "src/porteria/aplicacion/puertos/salida/hasheador.py"
      via: "implementación del puerto Hasheador"
      pattern: "Hasheador"
    - from: "src/porteria/aplicacion/servicios/sesion.py"
      to: "src/porteria/dominio/identidad/permisos.py"
      via: "la verificación de permiso ocurre en la aplicación, nunca en la CLI"
      pattern: "puede"
    - from: "tests/arquitectura/invariantes/inv_01_09.py"
      to: "tests/arquitectura/test_invariantes_de_codigo.py"
      via: "descubrimiento por pkgutil.iter_modules sobre tests/arquitectura/invariantes/"
      pattern: "INVARIANTES"
---

<objective>
Entregar la parte de identidad que la Fase 1 tiene que congelar: el modelo de usuarios, el
almacenamiento seguro de la credencial, los cuatro roles del negocio, la columna de autor en toda fila
auditable y el actor en la bitácora encadenada.

> **Nota de alcance, dicha con precisión.** D-18 —"hay usuarios con contraseña en v1"— es **alcance
> agregado por el usuario por encima de los 79 requerimientos v1**. Ningún requerimiento pide
> autenticar: las Fases 5, 7 y 9 piden **registrar** el operador, no autenticarlo. Se documenta como
> decisión del usuario y se ancla a NUC-05 porque la columna de autor y el actor de la bitácora
> encadenada se persisten **dentro de la misma transacción única** que la evidencia — que es
> literalmente lo que NUC-05 exige. La pantalla de inicio de sesión y la gestión completa de usuarios
> desde la interfaz están explícitamente diferidas a la fase de interfaz.

> **Corolario que hay que decir en voz alta.** Como el inicio de sesión es de la fase de interfaz, en la
> Fase 1 **el camino con sesión no es alcanzable desde el producto**: se ejercita por la API de
> aplicación en `tests/integracion/test_autoria_en_evidencia.py`, invocando `Sesion.iniciar` y
> `CasoDeUsoEjecutarCaptura` directamente. Desde la consola, `porteria capturar` registra con autor no
> identificado y punto.
>
> Se evaluó agregar `--operador` a `porteria capturar` para hacer la propiedad observable y **se
> descartó**, por dos razones concretas. Primera: una opción que declara el operador sin verificar
> credencial convertiría `autor_id` en un dato autodeclarado y no verificable — debilitaría justo la
> propiedad probatoria que NUC-05 existe para proteger, y una firma que cualquiera puede poner en la
> línea de comandos vale menos que ninguna firma. Segunda: pedir la contraseña en cada captura
> contradice D-26 (la sesión no se cierra sola, el cambio de operador es explícito) y obligaría a este
> plan a tocar `capturar.py` y `arranque.py`, que son del plan 01-07. La autoría verificable llega
> completa con la pantalla de inicio de sesión, no antes y no a medias.

La regla transversal que el usuario aplicó de forma consistente rige también acá: **registrar lo
incompleto en lugar de impedirlo.** La captura nunca se bloquea por falta de sesión; si no hay sesión
iniciada, la evidencia se registra con autor "no identificado", el hecho queda marcado y se contabiliza
en el panel de calidad de la Fase 7. Es la misma regla que rige la captura parcial y la evidencia
comprometida.

Implementa D-18 a D-26 completas: usuarios con contraseña, sólo el modelo y la credencial en esta fase,
cuatro roles fijos con permisos en código, captura sin sesión permitida, Argon2id con sal por usuario y
sin reglas de complejidad ni caducidad, sin credencial por defecto, identidad propia independiente del
usuario de Windows, desactivación en vez de borrado, y sesión que no se cierra sola.

Purpose: sin el rol de Compras en el esquema, la prueba automática de solo lectura que exige la Fase 9
no tiene contra qué correr; y sin el actor en la cadena, la bitácora encadenada de D-08 queda
incompleta desde el registro uno.
Output: dominio de identidad, hasheo Argon2id, repositorio, servicio de sesión, dos órdenes de
consola y los cinco invariantes de código que sostienen las propiedades de seguridad del plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-RESEARCH.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-CONTEXT.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-05-SUMMARY.md
@.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-07-SUMMARY.md
@src/porteria/dominio/auditoria/registro.py
@src/porteria/infraestructura/persistencia/sqlite/modelos.py
@src/porteria/cli/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Usuario, cuatro roles fijos, matriz de permisos aislada y el invariante de no borrado</name>
  <files>src/porteria/dominio/identidad/modelo.py, src/porteria/dominio/identidad/permisos.py, src/porteria/dominio/identidad/politica.py, tests/arquitectura/invariantes/inv_01_09.py, tests/dominio/test_identidad.py, tests/dominio/test_permisos.py</files>
  <read_first>
    - **Los paquetes de prueba ya existen.** El plan `01-01` (Tarea 1) crea los diez `tests/*/__init__.py`, incluidos `tests/`, `tests/arquitectura/`, `tests/arquitectura/invariantes/`, `tests/dominio/` y `tests/integracion/`. **Este plan no los crea ni los edita.**
    - `tests/arquitectura/test_invariantes_de_codigo.py` (plan `01-01`): descubre los módulos de `tests/arquitectura/invariantes/` con `pkgutil.iter_modules` y exige que cada uno exponga `INVARIANTES: tuple[Invariante, ...]`. Leer ahí la firma exacta de `Invariante` (archivo objetivo, patrón, cantidad máxima de coincidencias, `ignorar_comentarios`) antes de escribir la primera.
    - `.planning/phases/01-.../01-CONTEXT.md` §Decisions D-18 a D-26 completas, y D-54 (Windows es la plataforma de compuerta)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Open Questions" 4 ("congelar los 4 roles ahora pero **no** congelar la matriz de permisos; dejarla en un módulo de dominio aislado y con pruebas propias, para que evolucione sin tocar el esquema")
    - `.planning/phases/01-.../01-RESEARCH.md` § "Assumptions Log" A6 (los 4 roles se congelan sin haber validado los permisos concretos con Logística, Compras y Administración)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Security Domain" fila "Contraseña débil en un puesto compartido" (el único control es longitud mínima, más una lista corta de contraseñas prohibidas, que **no** es una regla de complejidad)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Security Domain" tabla ASVS fila V4 ("la verificación tiene que ocurrir en la **aplicación**, nunca en la CLI ni en la futura UI")
    - `src/porteria/dominio/comun/identificadores.py` (plan 01-02)
  </read_first>
  <behavior>
    - `Rol` tiene exactamente cuatro valores: `PORTERO`, `LOGISTICA`, `COMPRAS`, `ADMINISTRACION`. Agregar un quinto es un cambio deliberado con su migración.
    - `Usuario` lleva id opaco, nombre de usuario, rol, estado `activo`, y el nombre del usuario de Windows **sólo como dato de diagnóstico, sin valor de autoría**: en una portería la PC suele tener una única cuenta compartida por todos los turnos.
    - `usuario.desactivar(por, en)` deja `activo=False` y registra autor e instante. **No existe** un método de borrado: un usuario que ya firmó evidencia no se elimina, porque es la única opción coherente con una bitácora encadenada.
    - `permisos.puede(rol, accion)` responde desde una matriz en código. `COMPRAS` **no** puede editar, configurar ni eliminar nada — sin ese rol en el esquema, la prueba automática de solo lectura que exige la Fase 9 no tiene contra qué correr.
    - `politica.validar(contraseña, nombre_de_usuario)` exige **sólo** longitud mínima y rechaza una lista corta de prohibidas (`portero`, `porteria`, `123456`, `password`, y el propio nombre de usuario). **No hay** reglas de complejidad, ni caducidad, ni historial, ni bloqueo por intentos: en un puesto compartido esas reglas producen la contraseña escrita en un papel pegado al monitor.
    - La matriz de permisos vive en su propio módulo con sus propias pruebas, **separada del esquema**: los cuatro roles se congelan ahora porque agregar uno es una migración barata, pero la matriz tiene que poder evolucionar cuando Logística, Compras y Administración opinen, sin tocar la base.
    - `tests/arquitectura/invariantes/inv_01_09.py` expone `INVARIANTES` y aporta el invariante `identidad_sin_borrado`: cero coincidencias del patrón `def (borrar|eliminar)` en `src/porteria/dominio/identidad/modelo.py`, con `ignorar_comentarios=True`.
    - El invariante falla si su archivo objetivo no existe. Un invariante que se saltea cuando no encuentra su objetivo no verifica nada.
  </behavior>
  <action>
Escribir `dominio/identidad/modelo.py` con `Usuario` y `Rol` (los cuatro fijos), `politica.py` con la
validación mínima, y `permisos.py` con la matriz.

Los cuatro roles se congelan; la **matriz no**. Es la recomendación explícita de la investigación:
todavía no participaron los usuarios indirectos, y si los permisos reales no encajan, el costo de
cambiar la matriz tiene que ser cambiar un módulo de dominio con sus pruebas, no migrar un esquema.
Por eso la matriz no se persiste ni se configura: está en código, aislada y probada.

La política de contraseñas implementa D-22 literalmente. La única exigencia es longitud mínima. La
lista corta de contraseñas prohibidas **no** contradice la decisión: no es una regla de composición,
es un piso de sensatez, y es lo que OWASP recomienda por encima de las reglas de complejidad. El riesgo
residual —una contraseña débil en un puesto compartido— está aceptado y documentado por el usuario.

El nombre del usuario de Windows se guarda pero **no vale como autoría**. La identidad es propia y
también sirve para el caso inverso: la misma cuenta de Windows con tres operadores distintos en el
día.

`permisos.py` no debe conocer la CLI ni la interfaz. La verificación de permiso ocurre en la
**aplicación** (Tarea 3): si la CLI decidiera, la interfaz de la Fase 2 tendría que reimplementar la
misma decisión y las dos divergirían.

Crear `tests/arquitectura/invariantes/inv_01_09.py` con el primer invariante del plan. Las propiedades
de seguridad de este plan van como pruebas de Python y no como comandos de shell porque la compuerta
corre en Windows (D-54): un criterio expresado con una herramienta que puede no existir en la
plataforma no falla, simplemente no se ejecuta, y "no se pudo verificar" termina leyéndose como
"pasó". Para una propiedad de seguridad eso es peor que no tener criterio, porque da confianza falsa.

Dos reglas de higiene, porque son la forma conocida de que un invariante se invalide a sí mismo:

1. `ignorar_comentarios` descarta únicamente las líneas que empiezan con `#`, **no** los docstrings. La
   explicación de por qué un usuario no se borra va en comentarios `#` dentro de `modelo.py`, nunca en
   su docstring de módulo.
2. En `inv_01_09.py`, los términos prohibidos aparecen **sólo como dato del patrón**. Los comentarios
   que explican cada invariante no los repiten en prosa.
  </action>
  <verify>
    <automated>uv run pytest tests/dominio/test_identidad.py tests/dominio/test_permisos.py tests/arquitectura/test_invariantes_de_codigo.py -q && uv run lint-imports --no-cache</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/dominio/test_identidad.py tests/dominio/test_permisos.py -q` devuelve exit 0
    - `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` devuelve exit 0
    - `uv run lint-imports --no-cache` devuelve exit 0 (identidad sigue sin dependencias externas)
    - `Rol` tiene exactamente cuatro valores; una prueba lo afirma con `len(list(Rol)) == 4`
    - Una prueba afirma que `Rol.COMPRAS` no puede ninguna acción de escritura, configuración ni borrado
    - `tests/arquitectura/invariantes/inv_01_09.py` existe, expone `INVARIANTES` como tupla, y una prueba afirma que `inv_01_09` figura entre los módulos descubiertos por `test_invariantes_de_codigo.py`
    - El invariante `identidad_sin_borrado` devuelve 0 coincidencias de `def (borrar|eliminar)` sobre `src/porteria/dominio/identidad/modelo.py` con `ignorar_comentarios=True`
    - Una prueba afirma que `identidad_sin_borrado` **falla** si se lo apunta a un archivo de muestra que define uno de esos métodos fuera de un comentario `#`
    - Una prueba afirma que un invariante **falla** si su archivo objetivo no existe, en vez de saltearse
    - Los comentarios de `inv_01_09.py` no repiten en prosa los términos que sus patrones prohíben: una prueba afirma que cada patrón aparece una sola vez en el módulo, como dato
    - Existe una prueba que desactiva un usuario y afirma que `activo` es `False`, que la baja registró autor e instante, y que el nombre del usuario se conserva
    - Existe una prueba que afirma que `politica.validar` rechaza `portero`, `porteria`, `123456`, `password` y el propio nombre de usuario
    - Existe una prueba que afirma que `politica.validar` **acepta** una contraseña larga sin mayúsculas, sin números y sin símbolos (no hay reglas de complejidad)
    - `src/porteria/dominio/identidad/permisos.py` no importa nada de `porteria.cli` ni de `porteria.infraestructura`
    - El módulo de permisos contiene un comentario que registra que la matriz es evolucionable sin migración y que los cuatro roles no lo son
  </acceptance_criteria>
  <done>Los cuatro roles del negocio existen congelados, la matriz de permisos vive aislada y evolucionable, la política de contraseñas es la que el usuario decidió, y "un usuario no se borra" es un invariante de Python que corre en Windows.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Argon2id, repositorio de usuarios y el asistente de primer arranque</name>
  <files>src/porteria/aplicacion/puertos/salida/hasheador.py, src/porteria/aplicacion/puertos/salida/repositorio_usuarios.py, src/porteria/infraestructura/seguridad/argon2.py, src/porteria/infraestructura/persistencia/sqlite/repositorio_usuarios.py, src/porteria/cli/ordenes/crear_administrador.py, src/porteria/cli/ordenes/usuarios.py, tests/arquitectura/invariantes/inv_01_09.py, tests/integracion/test_credenciales.py</files>
  <read_first>
    - **`tests/arquitectura/invariantes/inv_01_09.py` ya existe**: lo crea la Tarea 1 de **este mismo plan**. Esta tarea lo **extiende** agregando invariantes a `INVARIANTES`, no lo reescribe.
    - `.planning/phases/01-.../01-RESEARCH.md` § "Security Domain" fila "Parámetros de Argon2id insuficientes" (defaults de `argon2-cffi` 25.1.0: `m=65536 KiB, t=3, p=4`, muy por encima del mínimo de OWASP; **201 ms medidos** en la máquina de investigación; usar el perfil `RFC_9106_LOW_MEMORY`, formato PHC, y `check_needs_rehash` en cada verificación exitosa)
    - `.planning/phases/01-.../01-RESEARCH.md` § "Don't Hand-Roll" fila "Hash de contraseñas" y § "State of the Art" (`argon2.__version__` está deprecado; usar `importlib.metadata.version("argon2-cffi")`)
    - `.planning/phases/01-.../01-CONTEXT.md` §Decisions D-19, D-22, D-23, D-24, D-25
    - `.planning/phases/01-.../01-RESEARCH.md` § "Security Domain" tabla ASVS fila V7 ("Nunca registrar la contraseña ni el hash")
    - `src/porteria/infraestructura/persistencia/sqlite/modelos.py` (tabla `usuario`, plan 01-05)
    - `src/porteria/infraestructura/runtime/redaccion.py` (plan 01-06: la lista de claves sensibles ya cubre `contraseña` y `credencial`)
    - `src/porteria/cli/app.py` (contrato de `cli/ordenes/`: **no editar `app.py`**)
  </read_first>
  <behavior>
    - `HasheadorArgon2.hashear(contraseña)` devuelve una cadena en **formato PHC** que empieza con `$argon2id$`. Dos hasheos de la misma contraseña producen cadenas distintas (sal por usuario).
    - `verificar(hash, contraseña)` devuelve `True` con la correcta y `False` con una incorrecta, sin lanzar excepción por el caso negativo.
    - `necesita_rehash(hash)` usa `check_needs_rehash`: permite subir los parámetros en el futuro **sin pedirle la contraseña a nadie**, porque los parámetros viajan dentro del hash PHC.
    - El tiempo de hasheo se mide y se reporta en `porteria estado`, porque 64 MiB y 4 hilos en una PC industrial vieja pueden pasar de 1 segundo y eso hay que saberlo antes de instalar, no después.
    - Con la base recién creada, **no existe ningún usuario**: no hay credencial por defecto en el producto. Una prueba lo afirma sobre una base virgen.
    - `porteria crear-administrador --usuario <nombre>` pide la contraseña sin eco, la valida contra la política, crea el usuario con rol `ADMINISTRACION` y **falla si ya existe algún usuario**: el asistente es de primer arranque, no una puerta trasera permanente. **El comportamiento de este prompt sin eco es contrato estable**: el plan 01-10 lo reutiliza como patrón para `porteria credenciales --sistema <s> --usuario <u>`.
    - `porteria usuarios --listar [--json]` lista nombre, rol y estado. **Nunca** muestra el hash.
    - `porteria usuarios --desactivar <nombre> --autor <nombre>` desactiva y deja el registro; no hay opción de borrado.
    - Ni la contraseña ni el hash aparecen en ninguna línea de la bitácora tras ejecutar el alta completa.
    - `inv_01_09.py` gana dos invariantes: `repositorio_usuarios_sin_borrado` (cero coincidencias de `def (borrar|eliminar)` en `src/porteria/infraestructura/persistencia/sqlite/repositorio_usuarios.py`) y `sin_api_de_version_deprecada` (cero coincidencias de `argon2.__version__` en `src/porteria/infraestructura/seguridad/argon2.py`), ambos con `ignorar_comentarios=True`.
  </behavior>
  <action>
Escribir `aplicacion/puertos/salida/hasheador.py` con `Hasheador` como `Protocol`
(`hashear`, `verificar`, `necesita_rehash`) y `repositorio_usuarios.py` con `RepositorioDeUsuarios`.

`infraestructura/seguridad/argon2.py` sobre `argon2-cffi` 25.1.0 con el perfil `RFC_9106_LOW_MEMORY`.
No escribir criptografía propia ni usar `hashlib.scrypt`: D-22 dice Argon2id explícitamente y es la
recomendación vigente de OWASP. El formato PHC guarda los parámetros dentro del hash, que es lo que
hace posible `check_needs_rehash` y por lo tanto migrar parámetros en una versión futura sin pedir la
contraseña a nadie.

Leer la versión del paquete con `importlib.metadata.version("argon2-cffi")`: la API de versión del
módulo está deprecada y emite `DeprecationWarning`. El invariante `sin_api_de_version_deprecada` lo
sostiene desde la compuerta.

`infraestructura/persistencia/sqlite/repositorio_usuarios.py` sobre la tabla `usuario` del plan 01-05,
en estilo SQLAlchemy 2.0 (`select()` + `scalars()`). El repositorio expone `crear`, `buscar_por_nombre`,
`listar`, `desactivar` y `contar` — y **ningún** método de borrado.

`cli/ordenes/crear_administrador.py`: el asistente de primer arranque de D-23. Pide la contraseña con
`typer.prompt(..., hide_input=True, confirmation_prompt=True)` y falla si `repositorio.contar() > 0`.
Que no exista ninguna credencial por defecto en el producto no es una preferencia: una credencial por
defecto en un producto que se instala en plantas de terceros es una vulnerabilidad conocida el día uno
de la venta. Mantener estable el comportamiento del prompt: el plan 01-10 lo toma como patrón para su
propia orden de credenciales, así que cambiarlo acá rompe allá.

`cli/ordenes/usuarios.py`: `--listar`, `--desactivar`, `--autor`, `--json`. Nunca muestra el hash, ni
siquiera bajo `--json`.

Al terminar, ejecutar el alta completa y **verificar sobre el archivo de bitácora real** que ni la
contraseña ni el hash aparecen en ninguna línea. El redactor del plan 01-06 ya cubre las claves
`contraseña`, `credencial` y el formato PHC; esta prueba demuestra que está enchufado también en este
camino.

Extender `inv_01_09.py` con los dos invariantes de esta tarea. Van acá y no en la Tarea 1 porque sus
archivos objetivo nacen en esta tarea, y un invariante que apunta a un archivo inexistente tiene que
fallar. Vale la misma higiene: la explicación de por qué el repositorio no borra va en comentarios `#`
de su módulo, no en el docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/integracion/test_credenciales.py tests/arquitectura/test_invariantes_de_codigo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integracion/test_credenciales.py -q` devuelve exit 0
    - Existe una prueba que afirma que el hash empieza con `$argon2id$` y que dos hasheos de la misma contraseña difieren
    - Existe una prueba que afirma que `verificar` devuelve `False` con la contraseña incorrecta **sin** lanzar excepción
    - Existe una prueba que afirma que `necesita_rehash` devuelve `True` ante un hash generado con parámetros más bajos
    - Existe una prueba sobre una base recién creada que afirma `repositorio.contar() == 0` (no hay credencial por defecto)
    - `uv run porteria crear-administrador --usuario admin` sobre una base con un usuario ya existente devuelve exit distinto de 0 con un mensaje que lo explica
    - Existe una prueba que afirma que el prompt de contraseña de `crear-administrador` no hace eco y pide confirmación (`hide_input=True`, `confirmation_prompt=True`): es el patrón que reutiliza el plan 01-10
    - `uv run porteria usuarios --listar --json` devuelve exit 0 y su JSON **no** contiene la subcadena `argon2`
    - Existe una prueba que ejecuta el alta completa y afirma que ni la contraseña en claro ni el hash aparecen en ninguna línea del archivo de bitácora
    - `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` devuelve exit 0 y `inv_01_09.INVARIANTES` tiene ahora exactamente 3 elementos
    - El invariante `repositorio_usuarios_sin_borrado` devuelve 0 coincidencias de `def (borrar|eliminar)` sobre `src/porteria/infraestructura/persistencia/sqlite/repositorio_usuarios.py` con `ignorar_comentarios=True`
    - El invariante `sin_api_de_version_deprecada` devuelve 0 coincidencias de `argon2.__version__` sobre `src/porteria/infraestructura/seguridad/argon2.py` con `ignorar_comentarios=True`
    - Una prueba afirma que cada uno de los dos invariantes nuevos **falla** contra un archivo de muestra que sí contiene su patrón fuera de un comentario `#`
    - `src/porteria/cli/app.py` no fue modificado por este plan
  </acceptance_criteria>
  <done>Las credenciales se protegen con Argon2id en formato PHC con sal por usuario, no existe credencial por defecto, ni la contraseña ni el hash pueden llegar a un archivo de bitácora, y las tres propiedades de código quedan afirmadas por invariantes ejecutables en Windows.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Sesión que no expira, autoría en la evidencia y actor en la cadena de auditoría</name>
  <files>src/porteria/aplicacion/servicios/sesion.py, src/porteria/aplicacion/casos_de_uso/gestionar_identidad.py, tests/arquitectura/invariantes/inv_01_09.py, tests/integracion/test_autoria_en_evidencia.py</files>
  <read_first>
    - **`tests/arquitectura/invariantes/inv_01_09.py` ya existe**: lo crean las Tareas 1 y 2 de este mismo plan. Esta tarea lo **extiende**, no lo reescribe.
    - `.planning/phases/01-.../01-CONTEXT.md` §Decisions D-19, D-21, D-24, D-26
    - `.planning/phases/01-.../01-RESEARCH.md` § "Security Domain" tabla ASVS filas V3 (la "sesión" es estado en memoria del proceso y no expira por diseño; sin token, sin cookie, sin superficie de fijación) y V4 (la verificación de permiso ocurre en la aplicación)
    - `src/porteria/aplicacion/casos_de_uso/ejecutar_captura.py` (plan 01-07). **Firma fijada por 01-07, no alterarla:** `CasoDeUsoEjecutarCaptura(reloj, fuente, codificador, almacen, uow, ventana_aceptacion_ms, calidad_jpeg_por_camara, antes_de_confirmar: Callable[[], None] | None = None)`. `antes_de_confirmar` es el gancho con el que esta tarea corta la transacción.
    - `src/porteria/dominio/auditoria/registro.py` (plan 01-02: el actor forma parte del registro encadenado)
    - `src/porteria/infraestructura/persistencia/sqlite/repositorios.py` (plan 01-07: `RepositorioDeAuditoria.encadenar`)
    - **Alcance:** este plan **no** toca `cli/ordenes/capturar.py` ni `composicion/arranque.py` (el composition root del plan 01-07; no existe ningún `cli/arranque.py`). Ver el corolario del `<objective>`: el camino con sesión se ejercita por API de aplicación, no desde la consola.
  </read_first>
  <behavior>
    - `Sesion` es estado **en memoria del proceso**: sin token, sin cookie, sin archivo. `iniciar(usuario)`, `cambiar_operador(usuario)`, `cerrar()`, `actor_actual() -> UsuarioId | None`.
    - La sesión **no expira sola**: no hay temporizador de inactividad ni corte por turno. Una expiración justo cuando el camión está sobre la balanza produciría evidencia firmada como no identificada en vez de firmada bien.
    - Cambiar de operador es explícito y deja registro en la bitácora encadenada.
    - Sin sesión iniciada, `ejecutar_captura` **se ejecuta igual**: la fila queda con `autor_id IS NULL` y con la marca de no identificada, y el registro de auditoría correspondiente queda con `actor_id IS NULL` y su acción marcada. **Éste es el único camino alcanzable desde la consola en la Fase 1.**
    - Con sesión iniciada —camino ejercitado **sólo por la API de aplicación**, ver el corolario del `<objective>`— la misma captura persiste `autor_id` **dentro de la misma transacción** que la evidencia, el manifiesto, la bitácora y el outbox. Una prueba que corta la transacción con `antes_de_confirmar` afirma que no queda ni la fila de evidencia ni el registro de auditoría con su actor.
    - `puede(accion)` consulta la matriz de permisos del dominio desde la **aplicación**. La CLI nunca decide un permiso por su cuenta.
    - Un usuario desactivado no puede iniciar sesión, y las firmas que ya dejó conservan su nombre al consultarlas.
    - `inv_01_09.py` gana dos invariantes: `sesion_sin_expiracion` (cero coincidencias, sin distinguir mayúsculas, del patrón `expira|timeout|inactividad` en `src/porteria/aplicacion/servicios/sesion.py`) y `consola_no_decide_permisos` (cero coincidencias de `permisos` en `src/porteria/cli/ordenes/usuarios.py`), ambos con `ignorar_comentarios=True`.
  </behavior>
  <action>
Escribir `aplicacion/servicios/sesion.py` con `Sesion` como estado en memoria. La decisión de que **no
se cierre sola** es del usuario y tiene una justificación operativa concreta: el nombre del operador
activo va a estar siempre visible en pantalla cuando llegue la interfaz, y el cambio es explícito. Una
expiración por inactividad en una portería produce exactamente el resultado que se quiere evitar —
evidencia firmada como no identificada — en el peor momento.

Escribir esa justificación en comentarios `#` dentro de `sesion.py`, **no** en el docstring de módulo:
`ignorar_comentarios` sólo descarta líneas `#`, así que un docstring que explique por qué la sesión no
expira contendría las mismas palabras que el invariante prohíbe y lo invalidaría. Es la trampa clásica
de una regla que se autoinvalida al documentarse.

Escribir `aplicacion/casos_de_uso/gestionar_identidad.py` con los casos de uso de alta de
administrador inicial, listado, desactivación y cambio de operador, todos exigiendo `Sesion` y
consultando `permisos.puede` **en la aplicación**. Si la verificación viviera en la CLI, la interfaz de
la Fase 2 tendría que reimplementarla y las dos divergirían — que es la clase de divergencia que
produce un agujero de control de acceso. Por eso `cli/ordenes/usuarios.py` no nombra la matriz ni
siquiera en su docstring: llama al caso de uso y muestra el resultado.

Escribir `tests/integracion/test_autoria_en_evidencia.py` ejercitando los dos caminos completos: con
sesión y sin sesión. El camino sin sesión es el que materializa D-21 y la regla transversal del
usuario: **registrar lo incompleto en lugar de impedirlo**. El sistema nunca frena la operación para
proteger su propia prolijidad; deja constancia y sigue.

El camino con sesión se ejercita construyendo `CasoDeUsoEjecutarCaptura` con la firma que fijó el plan
01-07 e inyectando la `Sesion` ya iniciada. **No** agregar una opción de operador a la consola: ver el
corolario del `<objective>`. Dejar constancia del alcance en el resumen del plan para que la
verificación de fase no lea la autoría con sesión como una capacidad disponible para el portero.

Incluir la prueba de transacción usando el gancho `antes_de_confirmar` de 01-07: con sesión iniciada,
cortar la transacción antes del commit y afirmar que no queda ni la fila de evidencia con su
`autor_id` ni el registro de auditoría con su `actor_id`. Es NUC-05 aplicado a la autoría: la firma y
el hecho firmado se guardan juntos o no se guarda ninguno.

Extender `inv_01_09.py` con los dos invariantes de esta tarea.
  </action>
  <verify>
    <automated>uv run pytest tests/integracion/test_autoria_en_evidencia.py tests/arquitectura/test_invariantes_de_codigo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integracion/test_autoria_en_evidencia.py -q` devuelve exit 0
    - Existe una prueba que ejecuta una captura sin sesión y afirma `autor_id IS NULL`, la marca de no identificada en la fila, y `actor_id IS NULL` en el registro de auditoría
    - Existe una prueba que ejecuta una captura con sesión —construyendo `CasoDeUsoEjecutarCaptura` con la firma de 01-07— y afirma que `autor_id` y `actor_id` quedan poblados con el mismo usuario
    - Existe una prueba que corta la transacción con `antes_de_confirmar` y sesión iniciada, y afirma que no quedó ni la fila de evidencia ni el registro de auditoría
    - Existe una prueba que afirma que la sesión **no** expira: tras simular un salto de tiempo largo, `actor_actual()` sigue devolviendo el mismo usuario
    - Existe una prueba que afirma que un usuario desactivado no puede iniciar sesión y que sus firmas anteriores conservan su nombre
    - Existe una prueba que afirma que el cambio de operador deja un registro en la bitácora encadenada
    - `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` devuelve exit 0 y `inv_01_09.INVARIANTES` tiene ahora exactamente 5 elementos
    - El invariante `sesion_sin_expiracion` devuelve 0 coincidencias de `expira|timeout|inactividad` (sin distinguir mayúsculas) sobre `src/porteria/aplicacion/servicios/sesion.py` con `ignorar_comentarios=True`; la justificación de por qué la sesión no expira está en comentarios `#` y una prueba afirma que `sesion.py` **sí** contiene al menos una línea `#` que la explica
    - El invariante `consola_no_decide_permisos` devuelve 0 coincidencias de `permisos` sobre `src/porteria/cli/ordenes/usuarios.py` con `ignorar_comentarios=True`
    - Una prueba afirma que `consola_no_decide_permisos` **falla** contra un archivo de muestra que sí importa la matriz fuera de un comentario `#`
    - `src/porteria/cli/ordenes/capturar.py` y `src/porteria/composicion/arranque.py` no fueron modificados por este plan: `git diff --name-only` no los lista. **Las dos rutas tienen que existir para que el criterio no pase por vacuidad** — `composicion/arranque.py` es el composition root que crea el plan 01-07; no existe ningún `cli/arranque.py`
    - `uv run porteria capturar --help` no ofrece ninguna opción de operador ni de autor: en la Fase 1 la consola sólo produce capturas con autor no identificado, y así está declarado en `must_haves`
  </acceptance_criteria>
  <done>La autoría llega a la evidencia y a la cadena de auditoría dentro de la misma transacción, la captura sigue sin bloquearse nunca por falta de sesión, y el alcance real de la autoría con sesión en la Fase 1 —sólo por API de aplicación— está declarado y verificado, no sobreentendido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → aplicación | La contraseña que un humano escribe en un puesto compartido |
| credencial almacenada → base local | El hash vive en la misma base que la evidencia |
| CLI / futura UI → verificación de permiso | La decisión de si una acción está permitida no puede vivir en el adaptador |
| criterio de aceptación → plataforma de compuerta | Un criterio que no puede ejecutarse en Windows no falla: se lee como "pasó" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-31 | Spoofing | credencial por defecto | **Alta** | mitigate | **No existe ninguna credencial por defecto en el producto** (D-23). El asistente de primer arranque crea el administrador inicial y falla si ya hay usuarios. Verificable: prueba sobre base virgen que afirma `contar() == 0`, y prueba que ejecuta el asistente sobre una base con usuario y exige que falle |
| T-01-10 | Spoofing | parámetros de Argon2id | Baja | mitigate | `argon2-cffi` 25.1.0 con perfil `RFC_9106_LOW_MEMORY` (`m=65536 KiB, t=3, p=4`), muy por encima del mínimo de OWASP (`m=19 MiB, t=2, p=1`). Formato PHC (los parámetros viajan dentro) + `check_needs_rehash` en cada verificación exitosa para poder subirlos sin pedir la contraseña. **Pendiente declarado:** medir el tiempo en el hardware real de portería — 64 MiB y 4 hilos en una PC industrial vieja podrían pasar de 1 s; el tiempo medido se reporta en `porteria estado` |
| T-01-09 | Spoofing | contraseña débil en puesto compartido | Media | **accept** | D-22 fija explícitamente sin complejidad ni caducidad, con la justificación correcta (el papel pegado al monitor). Controles compatibles: longitud mínima + lista corta de prohibidas (no es una regla de composición, es un piso de sensatez recomendado por OWASP por encima de las reglas de complejidad). **Riesgo residual aceptado y documentado por el usuario** |
| T-01-04 | Information Disclosure | contraseña y hash en la bitácora | **Alta** | mitigate | El redactor del plan 01-06 cubre `contraseña`, `credencial`, `password` y el formato PHC. Verificable de extremo a extremo: prueba que ejecuta el alta completa y afirma que ni la contraseña en claro ni el hash aparecen en **ninguna** línea del archivo de bitácora. `porteria usuarios --json` tampoco los expone |
| T-01-32 | Elevation of Privilege | verificación de permiso en el adaptador | Media | mitigate | `permisos.puede` se consulta desde la **aplicación**, nunca desde la CLI ni desde la futura interfaz. Verificable con el invariante `consola_no_decide_permisos` de `inv_01_09.py`, que corre bajo `pytest` en Windows y exige cero referencias a la matriz en `cli/ordenes/usuarios.py`, más una prueba negativa que demuestra que el invariante falla cuando debe. Si la CLI decidiera, la UI de la Fase 2 reimplementaría la decisión y las dos divergirían |
| T-01-33 | Repudiation | borrado de un usuario que firmó evidencia | Media | mitigate | No existe método de borrado, ni en el dominio ni en el repositorio: un usuario se **desactiva** y sus firmas conservan su nombre. Es la única opción coherente con una bitácora encadenada. Verificable con los invariantes `identidad_sin_borrado` y `repositorio_usuarios_sin_borrado` de `inv_01_09.py`, ejecutables en la compuerta de Windows |
| T-01-13 | Spoofing / Repudiation | captura sin sesión | Media | **accept** | D-21: la captura nunca se bloquea por falta de sesión. `autor_id = NULL` con la marca de no identificada, contabilizado en el panel de calidad de la Fase 7. Riesgo residual aceptado: es la misma regla que rige la captura parcial — registrar lo incompleto en lugar de impedirlo |
| T-01-34 | Session hijacking | sesión que no expira | Baja | **accept** | D-26. No hay sesión de red: es estado en memoria del proceso, sin token, sin cookie y sin superficie de fijación. Una expiración por inactividad justo cuando el camión está sobre la balanza produce evidencia firmada como no identificada en vez de firmada bien. Riesgo residual aceptado por el usuario |
| T-01-42 | Repudiation | criterio de seguridad inejecutable en la compuerta | Media | mitigate | Ninguna propiedad de seguridad de este plan se afirma con un comando de shell: las cinco viven en `tests/arquitectura/invariantes/inv_01_09.py` y las ejecuta `pytest`, que sí existe en Windows (D-54). Un criterio que no puede correr no falla, se lee como "pasó", y para una propiedad de seguridad eso es peor que no tener criterio. Cada invariante lleva su prueba negativa y la exigencia de fallar si su archivo objetivo no existe |
| T-01-43 | Spoofing | autoría autodeclarada desde la consola | Media | **accept** | Se descartó agregar `--operador` a `porteria capturar`: una opción que nombra al operador sin verificar credencial haría de `autor_id` un dato autodeclarado y no verificable, debilitando la propiedad probatoria que NUC-05 protege. Consecuencia aceptada y declarada en `must_haves`: en la Fase 1 la consola sólo produce capturas con autor **no identificado**, y la autoría verificable llega con la pantalla de inicio de sesión de la fase de interfaz |
</threat_model>

## Artifacts this phase produces (plan 01-09)

**Módulos:** `porteria.dominio.identidad.modelo`, `.permisos`, `.politica`, `porteria.aplicacion.puertos.salida.hasheador`, `.repositorio_usuarios`, `porteria.aplicacion.casos_de_uso.gestionar_identidad`, `porteria.aplicacion.servicios.sesion`, `porteria.infraestructura.seguridad.argon2`, `porteria.infraestructura.persistencia.sqlite.repositorio_usuarios`, `porteria.cli.ordenes.crear_administrador`, `.usuarios`

**Clases y protocolos:** `Usuario`, `Rol` (`PORTERO`, `LOGISTICA`, `COMPRAS`, `ADMINISTRACION`), `Hasheador` (Protocol), `HasheadorArgon2`, `RepositorioDeUsuarios` (Protocol), `RepositorioDeUsuariosSQLite`, `Sesion`, `Accion` (enum de acciones para la matriz de permisos)

**Funciones:** `politica.validar(contraseña, nombre_de_usuario)`, `permisos.puede(rol, accion)`, `hashear`, `verificar`, `necesita_rehash`, `Usuario.desactivar(por, en)`, `Sesion.iniciar`, `Sesion.cambiar_operador`, `Sesion.cerrar`, `Sesion.actor_actual`, `crear`, `buscar_por_nombre`, `listar`, `desactivar`, `contar`, `registrar(app)`

**Constantes:** `CONTRASENAS_PROHIBIDAS`, `LONGITUD_MINIMA_CONTRASENA`, perfil Argon2 `RFC_9106_LOW_MEMORY`

**Órdenes de consola nuevas:** `porteria crear-administrador` (opción `--usuario`, contraseña por prompt sin eco con confirmación — patrón reutilizado por el plan 01-10), `porteria usuarios` (opciones `--listar`, `--desactivar`, `--autor`, `--json`)

**Invariantes de código:** `tests/arquitectura/invariantes/inv_01_09.py` con `INVARIANTES` = (`identidad_sin_borrado`, `repositorio_usuarios_sin_borrado`, `sin_api_de_version_deprecada`, `sesion_sin_expiracion`, `consola_no_decide_permisos`), descubierto por `tests/arquitectura/test_invariantes_de_codigo.py` (plan 01-01)

**Campos poblados en el esquema existente:** `usuario.hash_credencial` (formato PHC), `usuario.rol`, `usuario.activo`, `usuario.usuario_windows` (sólo diagnóstico), `item_evidencia.autor_id`, `captura.autor_id`, `bitacora_auditoria.actor_id`

**No entregado en esta fase (declarado, no omitido):** inicio de sesión desde la consola u opción de operador en `porteria capturar`. El camino con sesión se ejercita por API de aplicación en `tests/integracion/test_autoria_en_evidencia.py`.

<verification>
- `uv run pytest tests/dominio/test_identidad.py tests/dominio/test_permisos.py tests/integracion/test_credenciales.py tests/integracion/test_autoria_en_evidencia.py -q` en verde
- `uv run pytest tests/arquitectura/test_invariantes_de_codigo.py -q` en verde con `inv_01_09` entre los módulos descubiertos y 5 invariantes aportados
- `uv run porteria usuarios --listar --json` no expone ningún hash
- El archivo de bitácora tras un alta completa no contiene la contraseña ni el hash
- `uv run lint-imports --no-cache` sigue en 4 contratos intactos con `dominio/identidad` poblado
</verification>

<success_criteria>
- D-18 a D-26 quedan implementadas en el alcance que la Fase 1 fija: modelo, credencial, roles, autoría en la fila y actor en la cadena
- La autoría se persiste dentro de la misma transacción única que la evidencia (NUC-05)
- El rol de Compras existe en el esquema, de modo que la prueba de solo lectura de la Fase 9 tenga contra qué correr
- La captura sigue sin bloquearse nunca por falta de sesión
- El alcance de la autoría con sesión está declarado sin ambigüedad: se ejercita por API de aplicación, la consola de la Fase 1 sólo produce autor no identificado, y la superficie de operador llega con la interfaz
- Ninguna propiedad de seguridad de este plan depende de una herramienta de shell ausente en Windows: las cinco son invariantes de `pytest`
</success_criteria>

<output>
Crear `.planning/phases/01-n-cleo-evidencia-trazable-y-contratos-externos-congelados/01-09-SUMMARY.md` al terminar.
</output>
